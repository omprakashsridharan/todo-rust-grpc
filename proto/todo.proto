syntax = "proto3";
package todo;

message TodoItem {
    string id = 1;
    // string description = 3;
    // string status = 4;
    // string created_at = 5;
    // string updated_at = 6;
}

message GetTodoRequest {
}

service Todo {
    rpc GetTodos(GetTodoRequest) returns (stream TodoItem);
}